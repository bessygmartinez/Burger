<div class="logo">
    <img src="/assets/images/logo.jpg" alt="Good Burger Logo">
</div>

<div class="row">
    <div class="col-md-12 text-center">
                <h2>Welcome to Good Burger, home of the Good Burger. Can I take your order?</h2>
    </div>
</div>

<div class="row">
    <div class="col-md-12 text-center mt-3">
        <p class="text">Eat a burger, dude. OR make a new one to eat!</p>
        <form class="form">
            <div class="form-group">
                <input type="text" placeholder="Burger name" id="burger-input" name="name">
            </div>
            <button id="submit-btn" type="submit">ADD <i class="fas fa-hamburger"></i></button>
        </form>
    </div>
</div>

<div class="row">

    <div class="col-md-5 eat">
        <h2>You can eat these, dude!</h2>
        <ul>
            {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devour=true}} {{/unless}} {{/each}}
        </ul>
    </div>
    <div class="col-md-2"></div>
    <div class="col-md-5 done">
        <h2>These burgers were already eaten, dude.</h2>
        <ul>
            {{#each burgers}} {{#if devoured}}
            <div class="eat-burger">
                <span class="label">#{{this.id}}</span>
                <span class="label">{{this.burger_name}}</span>
            </div>
            {{/if}} {{/each}}
        </ul>
    </div>
    <div class="col-md-1"></div>
</div>
