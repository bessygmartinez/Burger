<div class="eat-burger">
    
    <span class="label">#{{id}}</span>
    <span class="label">{{burger_name}}</span>
    <span class="label"><i class="fas fa-utensils"></i></span>
    <button data-id="{{id}}" data-newdevour="{{devour}}" class="eat-btn">Eat!</button>
</div>